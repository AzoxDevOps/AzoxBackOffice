<GenericDetail Model="Model"
               OnClose="OnClose"
               OnDelete="OnDelete"
               OnSave="OnSave">
    <BreadCrumb>
        <RadzenBreadCrumb>
            <RadzenBreadCrumbItem Text="Ürünler" Path="/admin/product/list" />
            <RadzenBreadCrumbItem Text="@(Model.IsNew ? "Yeni Ürün Ekle" : Model.Name)" />
        </RadzenBreadCrumb>
    </BreadCrumb>
    <Template>
        <div class="container-fluid">
            @if (Model.IsNew)
            {
                <MainTabContent />
            }
            else
            {
                <RadzenTabs>
                    <Tabs>
                        <RadzenTabsItem Text="Genel">
                            <MainTabContent />
                        </RadzenTabsItem>
                    </Tabs>
                </RadzenTabs>
            }
        </div>
    </Template>
</GenericDetail>